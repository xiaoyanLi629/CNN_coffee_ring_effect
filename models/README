Ten independent CNN models were created. The weights for each model were saved in one pkl file. For each of the ten independent CNN models, the accuracy and the missclassification results were saved for the last 200 iterations of the model, but the weights were only saved for the last iteration. 
